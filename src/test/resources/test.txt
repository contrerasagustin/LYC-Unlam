init {
    a1, a2 : Float;
    v1, v2, v3 : Int;
    p1, p2, p3 : String;
}
v3 := 100;
write ("ingrese un numero");
read  (v1);
write ("ingrese otro numero");
read (v2);
if ( v1 >= v3 ){
    repeat 10  (write("hola"););
}
else{
 while ( v3 >>  50 ){
    v2 := v2 + 1 ;
 }
 allequal([100,v2*122],[v2,v3]);
}